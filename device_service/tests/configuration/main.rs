#[cfg(test)]
mod configuration_test {
    #[test]
    fn connect() {
//        let configuration = Configuration::new(  );
//        let result = configuration.connect();
        assert!(false);
    }
}